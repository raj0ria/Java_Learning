package codingProblems.java8.filter;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

import static java.util.stream.Collectors.toList;

public class FilterDistictEvenNums {
    public static void main(String[] args) {
//        List<Integer> numbers = Arrays.asList(2, 4, 4, 6, 8, 10, 10, 12);
//
//        var list = numbers.stream().distinct().filter(n -> n%2==0).toList();
//        System.out.println(list);
//
//        var list2 = numbers.stream().collect(Collectors.filtering(n->n%2==0, Collectors.toList()));
//
//        List<String> stringList = Arrays.asList("Ashish", "Isha", "Sharma", "Rajoria");
//
//        var uppcase = stringList.stream().map(String::toUpperCase).collect(Collectors.toList());
//
//        System.out.printf(uppcase0);
//
//        numbers.stream().mapToInt(Integer::intValue).sum();
    }
}
